<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>No_Status_Reversal</fullName>
    <active>true</active>
    <description>you cannot change status from Approved to Pending/Rejected or from Rejected to Approved/Pending</description>
    <errorConditionFormula>AND(
  OR(
    TEXT(PRIORVALUE(Status__c)) = &quot;Approved&quot;, 
    TEXT(PRIORVALUE(Status__c)) = &quot;Rejected&quot;
  ),  
  ISCHANGED(Status__c)
)</errorConditionFormula>
    <errorMessage>you cannot change status from Approved to Pending/Rejected or from Rejected to Approved/Pending</errorMessage>
</ValidationRule>
