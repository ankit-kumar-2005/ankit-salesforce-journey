<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Application_Date_Not_Future</fullName>
    <active>true</active>
    <description>Application Date must not be in the future.</description>
    <errorConditionFormula>AND(
  NOT(ISBLANK(Application_Date__c)),
  Application_Date__c &gt; TODAY()
)</errorConditionFormula>
    <errorMessage>Application Date must not be in the future.</errorMessage>
</ValidationRule>
