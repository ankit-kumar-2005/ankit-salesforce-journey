<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Referred_By_If_Referral</fullName>
    <active>true</active>
    <description>If Source = Referral, then Referred By must be filled.</description>
    <errorConditionFormula>AND(
  ISPICKVAL(Source__c, &quot;Referral&quot;),
  ISBLANK(Referred_By__c)
)</errorConditionFormula>
    <errorMessage>If Source = Referral, then Referred By must be filled.</errorMessage>
</ValidationRule>
